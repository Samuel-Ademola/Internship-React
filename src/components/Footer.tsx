function Footer() {
  return (
    <footer className="footer">
      <div className="footer__content">
        <p>MovieHub • Clean movie discovery with TMDB, Firebase auth, and saved favourites.</p>
        <p>Built with React, Vite, Firebase, and TypeScript.</p>
      </div>
    </footer>
  );
}

export default Footer;
